apiVersion: v1
kind: Service
metadata:
  name: catalogue-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: catalogue-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
