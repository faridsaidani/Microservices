apiVersion: v1
kind: Service
metadata:
  name: commande-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: commande-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
