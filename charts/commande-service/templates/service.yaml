apiVersion: v1
kind: Service
metadata:
  name: commande-service
  labels:
    app.kubernetes.io/name: commande-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: commande-service
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8082
