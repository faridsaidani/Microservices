apiVersion: v1
kind: Service
metadata:
  name: paiement-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: paiement-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
